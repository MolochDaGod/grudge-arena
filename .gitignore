node_modules
dist
.vercel
.wrangler
.history
.venv
.venv-1

# Character assets live on Cloudflare R2 (assets.grudge-studio.com/arena/)
# Run: node scripts/sync-assets-r2.mjs to upload
# Run: node scripts/sync-assets-r2.mjs --dry-run to preview
public/assets/characters/*/
public/assets/maps/*/

# Forge master catalog (250MB) — source: D:\.downloads\30grudge6characters.glb via npm run import:forge
public/assets/forge/30grudge6characters.glb

# Animation packs are large — also on R2
# public/assets/animations/ is kept in git (small JSON manifests only)

.env*

# Raw Mixamo source (unzipped boxanimations.zip — run npm run pipeline:boxanims)
artifacts/boxanimations/
public/anims/staging/
.env*.local
